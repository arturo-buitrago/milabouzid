---
// Bio section. Markdown body is passed in via the `body` slot (rendered in the page from
// the `bio` collection). Photo + presskit download from content; Booking button → #booking.
import Button from './Button.astro';
import Flower from './Flower.astro';
import { isFilled } from '../lib/content';

interface Props {
  heading: string;
  photo?: string;
  presskitPdf?: string;
}
const { heading, photo, presskitPdf } = Astro.props;
const hasPhoto = isFilled(photo);
---

<section id="bio" class="bio">
  <div class="portrait">
    {hasPhoto ? <img src={photo} alt="Mila Bouzid" loading="lazy" /> : <div class="portrait-ph" aria-hidden="true" />}
  </div>

  <div class="body">
    <h2 class="heading">
      <Flower size="0.8em" color="var(--white)" />
      {heading}
    </h2>
    <div class="prose"><slot name="body" /></div>

    <div class="actions">
      <Button href={presskitPdf} variant="solid" disabled={!isFilled(presskitPdf)} download>
        Download artist profile &amp; presskit
      </Button>
      <Button href="#booking" variant="outline">Booking</Button>
    </div>
  </div>
</section>

<style>
  .bio {
    display: grid;
    gap: 1.5rem;
    padding: 3rem 1.25rem;
    max-width: 1100px;
    margin: 0 auto;
  }
  .portrait {
    border-radius: 18px;
    overflow: hidden;
  }
  .portrait img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .portrait-ph {
    aspect-ratio: 4 / 5;
    background: linear-gradient(135deg, var(--purple), var(--black-warm));
  }
  .heading {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0 0 1rem;
    font-family: var(--font-display);
    color: var(--yellow);
    font-size: clamp(1.6rem, 6vw, 2.4rem);
  }
  .prose {
    color: var(--white);
    opacity: 0.9;
    line-height: 1.65;
  }
  .prose :global(p) {
    margin: 0 0 1rem;
  }
  .actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }

  @media (min-width: 768px) {
    .bio {
      grid-template-columns: minmax(0, 0.9fr) minmax(0, 1.1fr);
      align-items: center;
      gap: 2.5rem;
      padding: 5rem 1.25rem;
    }
  }
</style>
